<%= Spree.t('order_mailer.return_reminder_email.dear_customer') %>

<%= Spree.t('order_mailer.return_reminder_email.instructions') %>

============================================================
<%= Spree.t('order_mailer.return_reminder_email.rental_summary') %>
============================================================
<% @order.line_items.rentable.each do |item| %>
  <%= item.quantity %> x <%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= raw(item.variant.options_text) -%>
<% end %>